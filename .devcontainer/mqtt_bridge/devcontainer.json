// https://aka.ms/devcontainer.json. 
{
	"name": "OVMS Admin MQTT Bridge",
	"initializeCommand": "docker compose -f ./docker-compose.yml -f ./docker-compose.dev.yml config > ./docker-compose.rendered.yml",
	"dockerComposeFile": [
		"../../docker-compose.rendered.yml"
	],
	"service": "mqtt_bridge",
	"workspaceFolder": "/home/worker",
	"remoteUser": "worker",
	"postCreateCommand": "bash",
	"customizations": {
		"vscode": {
			"extensions": [
				"ms-python.python"
			]
		}
	}
}
