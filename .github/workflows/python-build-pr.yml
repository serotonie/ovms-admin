name: Python Docker build PR

on:
  pull_request:
    branches:
      - main
    paths:
      - "python/**"
      - ".github/workflows/pr/docker/python-build-pr.yml"

permissions:
  contents: read
  packages: write

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  python-docker-build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v4

      - name: Login to GitHub Container Registry
        uses: docker/login-action@v4
        with:
          registry: ghcr.io
          username: ${{ github.repository_owner }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build Python image
        uses: docker/build-push-action@v7
        with:
          context: python
          target: trips_recorder
          platforms: linux/amd64
          push: false
          load: true
          cache-from: type=registry,ref=ghcr.io/serotonie/ovms-admin_trips-recorder:buildcache
          cache-to: type=registry,ref=ghcr.io/serotonie/ovms-admin_trips-recorder:buildcache,mode=max
